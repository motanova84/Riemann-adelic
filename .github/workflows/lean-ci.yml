name: lean-ci
on: [push, pull_request]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install elan
        run: curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y
      - name: Set up Lean toolchain
        run: |
          echo "$HOME/.elan/bin" >> $GITHUB_PATH
          cd formalization/lean && ~/.elan/bin/elan show
      - name: Generate lake manifest
        run: |
          cd formalization/lean
          ~/.elan/bin/lake update
      - name: Build Lean project
        run: |
          cd formalization/lean
          ~/.elan/bin/lake build
      - name: No extra axioms
        run: |
          cd formalization/lean
          ~/.elan/bin/lake exe print-axioms RiemannAdelic | tee axioms.txt
          ! grep -E ".+" axioms.txt || (echo "Found axioms"; cat axioms.txt; exit 1)
