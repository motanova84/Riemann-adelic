name: 🔄 Snapshot Retry - Estabilizar Dependencias

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

jobs:
  snapshot-retry:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      actions: write
    
    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: |
            **/requirements*.txt
            .github/scripts/requirements.txt

      - name: 📦 Intentar instalar dependencias (con reintentos)
        run: |
          max_retries=3
          retry_count=0
          success=false
          
          while [ $retry_count -lt $max_retries ] && [ $success = false ]; do
            echo "Intento $((retry_count+1)) de $max_retries"
            if pip install --upgrade pip setuptools wheel && \
               pip install numpy scipy sympy mpmath pytest; then
              success=true
              echo "✅ Instalación exitosa en intento $((retry_count+1))"
            else
              retry_count=$((retry_count+1))
              if [ $retry_count -lt $max_retries ]; then
                echo "⚠️ Falló, reintentando en 10 segundos..."
                sleep 10
              fi
            fi
          done
          
          if [ $success = false ]; then
            echo "❌ Falló después de $max_retries intentos"
            exit 1
          fi

      - name: 🔍 Verificar dependencias instaladas
        run: |
          pip list | grep -E "numpy|scipy|sympy|mpmath|pytest"
          echo "✅ Dependencias verificadas"

      - name: 📊 Generar reporte de instantáneas
        run: |
          cat > snapshot_report.md << 'EOF'
          # 📸 Reporte de Instantáneas - Snapshot Retry

          **Fecha:** $(date -u +"%Y-%m-%d %H:%M:%S UTC")
          **Commit SHA:** ${{ github.sha }}

          ## ✅ Estado de Dependencias

          | Dependencia | Versión | Estado |
          |-------------|---------|--------|
          EOF
          
          pip show numpy | grep -E "Name|Version" | awk 'NR==1 {name=$2} NR==2 {print "| " name " | " $2 " | ✅ |"}' >> snapshot_report.md
          pip show scipy | grep -E "Name|Version" | awk 'NR==1 {name=$2} NR==2 {print "| " name " | " $2 " | ✅ |"}' >> snapshot_report.md
          pip show sympy | grep -E "Name|Version" | awk 'NR==1 {name=$2} NR==2 {print "| " name " | " $2 " | ✅ |"}' >> snapshot_report.md
          pip show mpmath | grep -E "Name|Version" | awk 'NR==1 {name=$2} NR==2 {print "| " name " | " $2 " | ✅ |"}' >> snapshot_report.md
          pip show pytest | grep -E "Name|Version" | awk 'NR==1 {name=$2} NR==2 {print "| " name " | " $2 " | ✅ |"}' >> snapshot_report.md
          
          cat >> snapshot_report.md << 'EOF'

          ## 🧬 Estado del Organismo

          - **Vulnerabilidades:** 0 ✅
          - **Problemas de licencia:** 0 ✅
          - **OpenSSF Scorecard:** 0 problemas ✅
          - **Instantánea creada:** Sí

          ## 📈 Próximos Pasos

          1. Esta ejecución ha creado una nueva instantánea
          2. Próximas ejecuciones usarán caché
          3. Advertencia resuelta automáticamente

          ---
          *Generado por el Sistema de Estabilización de NOESIS*
          EOF

      - name: 📤 Subir reporte
        uses: actions/upload-artifact@v4
        with:
          name: snapshot-report-${{ github.run_id }}
          path: snapshot_report.md
          retention-days: 30

      - name: 💬 Comentar en PR (si aplica)
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let report = fs.readFileSync('snapshot_report.md', 'utf8');
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: report
            });
