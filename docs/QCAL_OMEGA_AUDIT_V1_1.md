# QCAL Ω Audit Ledger v1.1 — Riemann-adelic

This repository participates in the executable Ω evidence graph.

CI enforces `status(child) <= min(status(parent))`, validates dependency references and rejects cycles. Reported project claims are not upgraded by prose alone.

The current ledger intentionally distinguishes formalization from proof and prediction from experimental verification. The reported `ALL-GREEN` seal is stored as a reported project seal only; it is not treated as an independently verified cryptographic digest.

Operational entrypoints:

- `ledger/omega.json`
- `scripts/validate_omega_ledger.py`
- `.github/workflows/qcal-omega-audit.yml`
