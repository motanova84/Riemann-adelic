# Extended Requirements for V6.0 High-Precision Validation
# This file extends requirements.txt with additional dependencies
# for high-precision numerical validation and extended testing

# Core computational dependencies (high precision)
mpmath==1.3.0
numpy>=1.22.4,<2.3
scipy>=1.13.0
sympy==1.12

# Enhanced numerical libraries for extended precision
gmpy2>=2.1.5  # GMP multiple precision arithmetic (optional but recommended)

# Visualization for extended validation
matplotlib>=3.7.0

# Testing framework with coverage
pytest==8.2.0
pytest-cov==6.0.0
pytest-benchmark>=4.0.0  # For performance testing

# Data validation and integrity
hashlib  # Built-in, but explicitly noted for data integrity checks

# Jupyter for extended analysis (optional)
jupyter==1.0.0
nbconvert==7.16.4

# HTTP requests for extended data fetching
requests==2.32.3

# Development tools (optional, uncomment for development)
# flake8>=6.0.0
# black>=23.0.0
# isort>=5.12.0
# mypy>=1.0.0

# Note: For maximum precision in V6.0 validation:
# - Set mp.dps = 50 or higher in scripts
# - Use gmpy2 backend for mpmath: mp.mp.backend = 'gmpy'
# - Ensure sufficient memory for extended zero datasets
